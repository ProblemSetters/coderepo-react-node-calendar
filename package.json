{
    "name": "calendar",
    "version": "1.0.0",
    "private": true,
    "description": "React and Node calendar monorepo",
    "workspaces": [
        "frontend",
        "backend"
    ],
    "scripts": {
        "postinstall": "bash setup.sh --ensure-seeded",
        "prestart": "bash setup.sh --start",
        "start": "concurrently \"bun run dev:backend\" \"bun run dev:frontend\"",
        "dev:frontend": "cd frontend && bun run dev",
        "dev:backend": "cd backend && bun run dev",
        "seed": "cd backend && bun run seed",
        "clear": "rm -rf node_modules/ bun.lock && cd backend && rm -rf node_modules/ .env && cd .. && cd frontend && rm -rf node_modules/ .env && cd ..",
        "test:task1": "cross-env NODE_ENV=test CALENDAR_TEST_AUTH_BYPASS=true NODE_OPTIONS=--experimental-vm-modules JEST_JUNIT_OUTPUT_NAME=task1.xml jest --config=backend/jest.config.js --testPathPatterns=backend/__tests__/task1 --runInBand --passWithNoTests",
        "test": "cd frontend && bun run test && cd ../backend && bun run test",
        "test:coverage": "cd frontend && bun run test:coverage && cd ../backend && bun run test:coverage",
        "build": "cd frontend && bun run build"
    },
    "devDependencies": {
        "concurrently": "9.2.1",
        "cross-env": "7.0.3",
        "jest": "30.3.0"
    }
}
